# mcFoundation Theme Installation

# Install or clone the theme into your WordPress theme directory. Usually wp-content/themes.
# Run `npm install`
# Run `composer install`
# Run `bower install`
# Run `grunt build`
